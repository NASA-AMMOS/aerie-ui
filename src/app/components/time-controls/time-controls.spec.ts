import { TimeControlsComponent } from './time-controls.component';

describe('TimeControlsComponent', () => {
  let comp: TimeControlsComponent;

  beforeEach(() => {
    comp = new TimeControlsComponent();
  });

  it('should be defined', () => {
    expect(comp).toBeDefined();
  });
});
