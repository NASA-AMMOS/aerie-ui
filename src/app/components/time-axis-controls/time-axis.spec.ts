import { TimeAxisControlsComponent } from './time-axis-controls.component';

describe('TimeAxisControlsComponent', () => {
  let comp: TimeAxisControlsComponent;

  beforeEach(() => {
    comp = new TimeAxisControlsComponent();
  });

  it('should be defined', () => {
    expect(comp).toBeDefined();
  });
});
