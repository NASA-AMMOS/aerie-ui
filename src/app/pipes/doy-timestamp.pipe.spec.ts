import { DoyTimestampPipe } from './doy-timestamp.pipe';

describe('DoyTimestampPipe', () => {
  it('create an instance', () => {
    const pipe = new DoyTimestampPipe();
    expect(pipe).toBeTruthy();
  });
});
