<div>
  <slot />
</div>

<style>
  div {
    align-items: center;
    background: var(--primary);
    color: var(--primary-background-color);
    display: flex;
    font-size: 1rem;
    justify-content: space-between;
    padding: 1rem;
  }
</style>
