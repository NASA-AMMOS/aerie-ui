export enum TIME_MS {
  MILLISECOND = 1,
  SECOND = 1000,
  MINUTE = 60000,
  HOUR = 3600000,
  DAY = 86400000,
  MONTH = 2629800000,
  YEAR = 31557600000,
}
export enum TimeTypes {
  ABSOLUTE = 'absolute',
  EPOCH = 'epoch',
  EPOCH_SIMPLE = 'epoch_simple',
  RELATIVE = 'relative',
  RELATIVE_SIMPLE = 'relative_simple',
}
